# Scenario for test_trim_neutral_sim (full closed-loop TRIM, estimator in
# the loop). Convergence thresholds live in the test code itself, not here.

# Fixed seed so the derived noise/fault RNG streams are reproducible.
seed: 1
