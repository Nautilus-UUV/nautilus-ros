# Scenario for test_trim_neutral_sim + test_trim_neutral_sim_gt.
#
# Ppaired EKF/GT tests share thresholds: both tests
# load THIS file so they exercise an identical control + plant config.
# Threshold differences live in the test code itself, not here.

# Fixed seed so fault timing (if enabled) is reproducible run-to-run.
seed: 1

rig:
  faults:
    bcu_rpm:
      probability_per_sec: 0.0
      duration_sec: 0.0
