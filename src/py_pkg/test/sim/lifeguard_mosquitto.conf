# Throwaway broker for test_lifeguard_sim.py. Port 1884 so a dev broker on
# the default 1883 is never touched. mosquitto >=2.0 rejects anonymous
# clients on ad-hoc (-p) listeners, hence this explicit config.
listener 1884 127.0.0.1
allow_anonymous true
